"""Frozen clean PPO configuration for the S2-03T contact redesign."""

from agile.rl_env.rsl_rl import (
    RslRlOnPolicyRunnerCfg,
    RslRlPpoActorCriticCfg,
    RslRlPpoAlgorithmCfg,
)
from isaaclab.utils import configclass


@configclass
class S203TPPORunnerCfg(RslRlOnPolicyRunnerCfg):
    seed = 42
    device = "cuda:0"
    num_steps_per_env = 24
    max_iterations = 1200
    save_interval = 100
    experiment_name = "s2_03t_reachable_contact_curriculum"
    run_name = ""
    logger = "tensorboard"
    empirical_normalization = True
    clip_actions = 1.0
    enable_entropy_coef_annealing = False
    resume = False
    load_run = None
    load_checkpoint = None
    load_optimizer = False
    policy = RslRlPpoActorCriticCfg(
        init_noise_std=1.0,
        actor_hidden_dims=[256, 128, 64],
        critic_hidden_dims=[256, 128, 64],
        activation="elu",
    )
    algorithm = RslRlPpoAlgorithmCfg(
        value_loss_coef=1.0,
        use_clipped_value_loss=True,
        clip_param=0.2,
        entropy_coef=0.005,
        num_learning_epochs=5,
        num_mini_batches=4,
        learning_rate=1.0e-3,
        schedule="adaptive",
        gamma=0.99,
        lam=0.95,
        desired_kl=0.01,
        max_grad_norm=1.0,
        normalize_advantage_per_mini_batch=False,
        symmetry_cfg=None,
        rnd_cfg=None,
        l2c2_cfg=None,
        reward_normalization_cfg=None,
    )
